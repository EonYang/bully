var defaultSpeed = 5 ;

var users = [];
var groups = [];

class USER {
  constructor(id) {
    id = "",
    name ="",
    isAlive: 1;

    x, y;
    speed = defaultSpeed;

    kill = 0;
    die = 0;
  }
}

class GROUP {
  constructor(user1Id, user1Id) {
    users = [user1Id,user1Id];
    x,y,dia;
    power = this.users.length;
  }
}
